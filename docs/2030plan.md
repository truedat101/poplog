# 2030plan

Working plan for moving Pop-11/Poplog from historical footnote to a name
people find, try, and talk about.  Grounded in a codebase audit
(2026-08-09).  Shareable rendering:
<https://claude.ai/code/artifact/8f67d49d-ecb6-4ba9-99ec-8f4f8a9cd409>.
Editor-bindings detail lives in `../poplog-language-binding-plugins.md`.

**Core finding: Poplog's problem is not capability — it is
invisibility.**  The system beats CPython 3–20× on real workloads,
self-hosts on RISC-V, and carries forty years of AI teaching material.
But its documentation is invisible to search engines and LLMs, nothing
highlights `.p` files, there is no way to try it without building it,
and its most novel property — live native compilation — has no public
demonstration.

Effort scale: **S** ≈ days · **M** ≈ 1–3 weeks · **L** ≈ 1–2 months ·
**XL** ≈ quarter+.

## The unfair advantages (what everything leans on)

1. **Live native compilation** — redefine procedures in a running
   process, at machine speed; measured 3–20× CPython (BENCHMARKS.md).
2. **Five languages, one heap** — Pop-11, Prolog, Common Lisp, SML,
   plus the new native Forth, interoperating on one VM/GC.
3. **Tiny and portable** — 2.5–3.7 MB seed binaries; self-hosts on a
   RISC-V SBC; nix flake; four released corepops.
4. **Saved images** — checkpoint state + native code, restore in
   milliseconds (~190 KB / ~8 ms measured in the pop11 skill).
5. **Provenance and corpus** — the real lineage of British AI; the
   Birmingham/Sussex teaching material one `fetch-learning.sh` away.

## Tier 1 — Make it findable and tryable

| # | Item | Leverage / Effort |
| --- | --- | --- |
| 1.1 | **Web documentation portal + `llms.txt`.** Generator **shipped 2026-08**: `tools/gen-docs.sh` (a Pop-11 program on the Tier-2 stdlib) renders the 921-file HELP/TEACH/REF corpus to a static site with 5,300+ resolved cross-references + `llms.txt`, in 0.8 s. Remaining: host it (GitHub Pages or poplog.iotone.co), submit to search engines. | highest / generator done, hosting open |
| 1.2 | **Editor support** — tree-sitter grammar → GitHub highlighting via linguist; Neovim/Zed/VS Code clients + an LSP server written in Pop-11. Full design: `poplog-language-binding-plugins.md`. **Grammar core shipped 2026-08**: [IoTone/tree-sitter-pop11](https://github.com/IoTone/tree-sitter-pop11) — 95.2 % of the full 1,805-file tree parses clean (modern libraries 100 %), with highlight/fold/textobject queries and corpus tests. **Neovim + VS Code clients shipped 2026-08** (`editors/`), and the **LSP server shipped 2026-08**: `pop/lsp/pop11_lsp.p` (in Pop-11, over lib json) — real-compiler diagnostics via `pop_syntax_only` (nothing in the buffer executes), HELP/REF hover, live-dictionary completion; auto-started by the Neovim plugin, E2E-tested (`tools/lsp/test-e2e.py`), vendored into the release tarballs with a CI protocol smoke. **Zed extension shipped 2026-08-15** (`editors/zed/`, dev-extension form; registry submission needs a standalone repo) and **linguist PR filed 2026-08-15**: [github-linguist/linguist#8125](https://github.com/github-linguist/linguist/pull/8125) (Pop-11 + `.p` heuristics + samples + tree-sitter-pop11 grammar submodule; usage evidence ~7.3k files though mirror-heavy — acceptance is the maintainers' call). | high / all client work done, linguist pending review |
| 1.3 | **Jupyter kernel** — thin adapter over `popsession` (persistent engine + sentinels already solved). Turns the teaching corpus into runnable notebooks. **Shipped 2026-08**: `tools/jupyter/` (~150-line ipykernel wrapper + installer that self-provisions a venv); cross-cell persistence, native-compiled defines, mishaps survive as red cells; `examples/notebooks/pop11-live.ipynb` is executed through the kernel for real (committed outputs are genuine). TEACH conversions started 2026-08: `tools/jupyter/teach2nb.py` (mechanical converter: prose→markdown, indented code→cells, expected-output stripping, cross-prose define/loop merging, non-terminating-demo fencing) with TEACH LISTS/ARITH/DEFINE shipped as executed notebooks — 133 live cells; the remaining red cells are the files' own intentional error demonstrations. | high / kernel + first conversions done |
| 1.4 | **Browser playground, staged** — (a) server-backed over popsession (days); (b) existing build under in-browser emulation (weeks); (c) true wasm backend via syscomp (months — the riscv64 port proves backends are addable). Ship (a) early. | highest / S→XL |

## Tier 2 — Table-stakes standard library — **SHIPPED 2026-08**

Pattern set by `lib json` + `lib crypto`: pure Pop-11 where possible,
small C shim where not, HELP + must-fail tests always.  The whole tier
landed 2026-08: eleven libraries, 223 checks green via
`tools/test-libs.sh` (suites in `tools/tests/`).  The follow-ups
closed too: **`lib http_client`** (popcurl v0.2 in-tree — headers,
status, timeouts; its suite runs the Pop-11 client against the Pop-11
server) and **`lib pcre`** (PCRE2 shim — real modern regex syntax with
capture groups; `lib regexp` stays as the zero-dependency sibling).
Bonus fix unearthed en route: Poplog's interposed malloc served
dlopen from a fixed 160 KB static arena and SIGSYS-killed the process
when modern dependency chains exhausted it — now 8 MB of lazy bss
(`c_core.c`), unblocking every future large shim.

| # | Item | Status |
| --- | --- | --- |
| 2.1 | **`lib shell`** — `shell_run[_full]`/`shell_lines` with decoded status + stderr, background jobs (`shell_bg`/`shell_wait`/`shell_kill`), watchdog `shell_timeout`. HELP shell_lib. | ✅ shipped |
| 2.2 | **`lib http_server`** — HTTP/1.1 over `unix_sockets`, request property + `http_text`/`http_json` responses, 404/500 safety nets; `examples/http_hello.p` is a live curl-tested service. HELP http_server. **`lib http_client`** completes the pair (2026-08). | ✅ shipped (both) |
| 2.3 | **`lib utf8`** — strict RFC 3629 validate/length/code/substring/explode/cons by code point. HELP utf8. | ✅ shipped |
| 2.4 | **`lib regexp`** — search/matches/first/all/split/replace over the core Ved engine (`@`-syntax), compiled-pattern cache. HELP regexp_lib. **`lib pcre`** adds full modern syntax via a PCRE2 shim (2026-08). | ✅ shipped (both) |
| 2.5 | **`lib poptest`** — check/check_mishaps/test_summary; exit status reflects the verdict (`pop_exit_ok`). HELP poptest. | ✅ shipped |
| 2.6 | **Quick-wins batch** — `strutils`, `fileutils`, `csv` (RFC 4180), `datetime` (ISO 8601 UTC, pure integer math). HELP each. | ✅ shipped |

## Tier 3 — The story that makes it known

| # | Item | Leverage / Effort |
| --- | --- | --- |
| 3.1 | **Agent-native scripting: publish the pop11 skill + an MCP server.** The pitch no other language can make in 2026: an agent's helpers compile to native code mid-session (~180× measured over CLI spawns) and checkpoint to a 190 KB image. **MCP server shipped 2026-08, written in Pop-11 itself** (`pop/mcp/pop11_mcp.p` + `tools/pop11-mcp`, on `lib json`): the server IS the live session — measured 102 JSON-RPC eval round-trips in 0.04 s wall including startup (~0.4 ms each); tools pop11_eval / pop11_help (HELP/REF/TEACH lookup) / pop11_checkpoint with the verify gate / pop11_state; mishaps survive with diagnostics returned; `--restore image.psv` resumes a checkpoint (compiled procedures included) and answers a fresh handshake. Register: `claude mcp add pop11 -- $POPLOG/tools/pop11-mcp` (or the repo's `.mcp.json`). Remaining: publish the story (posts, registry listings). | highest / server done |
| 3.2 | **Live-coding showcase** — redefine the render procedure while the cube spins (native gfx + incremental compiler); one good screencast. | high / S |
| 3.3 | **"Five languages, one heap" demonstration** — Prolog rules over Pop-11 objects feeding an SML-typed core, in a notebook. Make the historical claim runnable. | medium / S–M |
| 3.4 | **Embedding API (`libpoplog`)** — C→Pop-11 callbacks exist today (`exfunc_export`, `callback.h`, REF EXTERNAL); what's missing is C/C++ as the host embedding Poplog. Targets the Lua niche with native-compile as the differentiator. | medium / L |

## Tier 4 — Trust and reach infrastructure

| # | Item | Leverage / Effort |
| --- | --- | --- |
| 4.1 | **Per-PR CI matrix** — only the corepops release workflow exists. Build + run json/crypto/validate suites on x86-64/ARM64/macOS per PR. The riscv64 bootstrap found exactly the regression class CI catches. | high / S |
| 4.2 | **Distribution ladder** — prebuilt engine tarballs per release, Homebrew tap, Docker + devcontainer (Codespaces = free playground v0), Debian PPA later. Nix already done. | medium / S–M per rung |
| 4.3 | **Windows native port** — real demand, XL effort; WSL2 covers the interim. Sequence after the wasm playground. | medium / XL |
| 4.4 | **Localization — Japanese first (`README.ja.md`).** Japan has one of the deepest Lisp/Prolog cultures anywhere (Fifth Generation Prolog era → today's Lisp community), and Poplog ships both languages on one heap. The tree already carries EUC-JP/Shift-JIS codecs — this was once a priority; make it one again. First cut — top-level `README.ja.md` — **shipped 2026-08**.  Next: **a Japanese Wikipedia article for POP-11** — none exists (ja.wikipedia 404s, verified 2026-08), while [en:POP-11](https://en.wikipedia.org/wiki/POP-11) is substantial (15+ refs) and translation of an established article is the accepted ja.wikipedia path.  Do it by the book: follow the ja translation guidelines with source-revision attribution in the edit summary (履歴継承, required by CC BY-SA), keep it neutral and historically sourced (the article is about the language, not this fork), and preferably recruit/pair with a ja community editor rather than maintainers self-publishing (COI norms) — the same person could review `README.ja.md`.  Link via Wikidata for interlanguage.  Community outreach underway: [Qiita post](https://qiita.com/truedata_iotone/items/ee56455f14ec6461d185) (2026-08) introducing the revived fork to the Japanese community — the natural place to recruit the Wikipedia/README reviewer.  Then: localized docs-portal pages (1.1) and first-class Japanese text via the UTF-8 layer (2.3). | medium / S (first cut done) |
| 4.5 | **Community surface** — CONTRIBUTING.md, good-first-issues (2.6 is ready-made), show-and-tell; TEACH JSON is the documented contributor on-ramp. | medium / S |

## A 90-day sequence

1. **Docs portal + llms.txt** (1.1) — everything else gets easier once
   the system is legible to search and AI.
2. **Tree-sitter grammar → GitHub highlighting** (1.2 / P1 of the
   bindings plan) — the repo starts looking alive.
3. **Jupyter kernel + server-backed playground** (1.3, 1.4a) —
   try-it-now exists.
4. **`lib shell` + `lib http_server`** (2.1, 2.2) — the "build
   something real" demo becomes writable.
5. **Publish the skill as an MCP server + two showcase posts**
   (3.1, 3.2) — the story ships.

Everything above is small-team-sized; the two big rocks (wasm backend,
Windows port) stay on the roadmap but gate nothing.  `README.ja.md`
(4.4 first cut) can slot in anywhere — it is independent of the rest.

## What *not* to do

* **Don't chase Python-ecosystem parity.**  Poplog wins on live-ness,
  size, and the multi-language heap.  Interop (shims, MCP) beats
  reimplementation.
* **Don't modernize away the weirdness.**  The open stack, ved, TEACH
  files — the strangeness is the story.  Frame it; don't sand it off.
* **Don't lead with benchmarks.**  Strong support, bad headline:
  "faster than CPython" starts arguments; "redefine native code while
  it runs" starts demos.

---

*Maintained alongside the shareable artifact; update both when the plan
changes.  History: created 2026-08 from the codebase audit; localization
tier added 2026-08 (Japanese first).*
